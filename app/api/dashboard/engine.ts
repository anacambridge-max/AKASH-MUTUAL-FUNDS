type Point={date:string;value:number};
type Group='BROAD'|'SECTORAL'|'THEMATIC';
type FundCfg={code:string;name:string;category:string;proxies:string[]};
type IndexRow={name:string;value:number|null;change:number|null;group:Group;status:'LIVE'|'UNAVAILABLE';source:'NSE'|'YAHOO'|'UNAVAILABLE'};

export const FUNDS:FundCfg[]=[
 {code:'120843',name:'Quant Flexi Cap Fund Direct Growth',category:'Diversified',proxies:['NIFTY 50','NIFTY SERVICES SECTOR']},
 {code:'120826',name:'Quant Large and Mid Cap Fund Direct Growth',category:'Large & Mid Cap',proxies:['NIFTY 50','NIFTY MIDCAP 150','NIFTY NEXT 50']},
 {code:'120821',name:'Quant Multi Asset Fund Direct Growth',category:'Multi Asset',proxies:['NIFTY 50','NIFTY BANK','NIFTY 500']},
 {code:'120823',name:'Quant Multi Cap Fund Direct Growth',category:'Multi Cap',proxies:['NIFTY 50','NIFTY NEXT 50','NIFTY MIDCAP 150']},
 {code:'120833',name:'Quant Infrastructure Fund Direct Growth',category:'Infrastructure',proxies:['NIFTY INFRASTRUCTURE','NIFTY CAPITAL GOODS','NIFTY CEMENT','NIFTY POWER','NIFTY CONSTRUCTION']},
 {code:'151791',name:'Quant BFSI Fund Direct Growth',category:'Banking & Financial',proxies:['NIFTY FINANCIAL SERVICES','NIFTY BANK','NIFTY PRIVATE BANK','NIFTY PSU BANK','NIFTY NBFC','NIFTY INSURANCE']},
 {code:'119827',name:'SBI Nifty 50 Index Fund Direct Growth',category:'Nifty 50',proxies:['NIFTY 50']},
 {code:'119783',name:'SBI Healthcare Opportunities Fund Direct Growth',category:'Healthcare',proxies:['NIFTY PHARMA','NIFTY HEALTHCARE','NIFTY500 HEALTHCARE']},
 {code:'119727',name:'SBI Focused Equity Fund Direct Growth',category:'Focused Equity',proxies:['NIFTY 50','NIFTY FINANCIAL SERVICES','NIFTY IT']},
 {code:'148490',name:"SBI Children's Benefit Fund Direct Growth",category:'Children',proxies:['NIFTY 50','NIFTY CONSUMER SERVICES']},
 {code:'147946',name:'Bandhan Small Cap Fund Direct Growth',category:'Small Cap',proxies:['NIFTY SMALLCAP 250','NIFTY SMALLCAP 100','NIFTY MIDSMALLCAP 400']},
 {code:'118989',name:'HDFC Mid Cap Opportunities Fund Direct Growth',category:'Mid Cap',proxies:['NIFTY MIDCAP 150','NIFTY MIDCAP 100','NIFTY MIDCAP 50']},
 {code:'143341',name:'UTI Nifty Next 50 Index Fund Direct Growth',category:'Nifty Next 50',proxies:['NIFTY NEXT 50']},
 {code:'150714',name:'UTI Gold ETF FoF Direct Growth',category:'Gold',proxies:['NIFTY COMMODITIES']},
 {code:'125497',name:'SBI Small Cap Fund Direct Growth',category:'Small Cap',proxies:['NIFTY SMALLCAP 250','NIFTY SMALLCAP 100','NIFTY MIDSMALLCAP 400']},
 {code:'120586',name:'ICICI Prudential Value Discovery Fund Direct Growth',category:'Value',proxies:['NIFTY 50','NIFTY FINANCIAL SERVICES','NIFTY PRIVATE BANK','NIFTY REALTY']},
 {code:'120503',name:'Axis ELSS Tax Saver Fund Direct Growth',category:'ELSS',proxies:['NIFTY 50']},
 {code:'144835',name:'Sundaram Services Fund Direct Growth',category:'Services',proxies:['NIFTY SERVICES SECTOR','NIFTY CONSUMER SERVICES','NIFTY COMMERCIAL & TRANSPORT SERVICES']},
 {code:'135800',name:'Tata Digital India Fund Direct Growth',category:'Technology',proxies:['NIFTY IT','NIFTY MIDSMALL IT & TELECOM','NIFTY TELECOMMUNICATIONS']}
];

const INDEXES:[string,Group][]=[
 ...['NIFTY 50','NIFTY NEXT 50','NIFTY 100','NIFTY 200','NIFTY 500','NIFTY MIDCAP 50','NIFTY MIDCAP 100','NIFTY MIDCAP 150','NIFTY SMALLCAP 50','NIFTY SMALLCAP 100','NIFTY SMALLCAP 250','NIFTY MIDSMALLCAP 400'].map(x=>[x,'BROAD']),
 ...['NIFTY BANK','NIFTY FINANCIAL SERVICES','NIFTY FINANCIAL SERVICES 25/50','NIFTY FINANCIAL SERVICES EX-BANK','NIFTY IT','NIFTY HEALTHCARE','NIFTY PHARMA','NIFTY AUTO','NIFTY FMCG','NIFTY METAL','NIFTY MEDIA','NIFTY REALTY','NIFTY PRIVATE BANK','NIFTY PSU BANK','NIFTY OIL & GAS','NIFTY CONSUMER DURABLES','NIFTY CAPITAL GOODS','NIFTY POWER','NIFTY TELECOMMUNICATIONS','NIFTY RETAIL','NIFTY INSURANCE','NIFTY NBFC','NIFTY CEMENT','NIFTY CHEMICALS','NIFTY CONSTRUCTION','NIFTY CONSUMER SERVICES','NIFTY COMMERCIAL & TRANSPORT SERVICES','NIFTY HOSPITALS','NIFTY HOUSING FINANCE','NIFTY REITS & REALTY','NIFTY500 HEALTHCARE','NIFTY MIDSMALL FINANCIAL SERVICES','NIFTY MIDSMALL HEALTHCARE','NIFTY MIDSMALL IT & TELECOM'].map(x=>[x,'SECTORAL']),
 ...['NIFTY INFRASTRUCTURE','NIFTY SERVICES SECTOR','NIFTY ENERGY','NIFTY COMMODITIES'].map(x=>[x,'THEMATIC'])
] as [string,Group][];

const Y:Record<string,string>={
 'NIFTY 50':'^NSEI','NIFTY NEXT 50':'^NSMIDCP','NIFTY 100':'^CNX100','NIFTY 200':'^CNX200','NIFTY 500':'^CRSLDX','NIFTY MIDCAP 50':'^NSEMDCP50','NIFTY MIDCAP 100':'^CNXMDCP','NIFTY MIDCAP 150':'^CNXMDCP150','NIFTY BANK':'^NSEBANK','NIFTY FINANCIAL SERVICES':'^NSEFIN','NIFTY IT':'^CNXIT','NIFTY HEALTHCARE':'^CNXHEALTH','NIFTY PHARMA':'^CNXPHARMA','NIFTY AUTO':'^CNXAUTO','NIFTY FMCG':'^CNXFMCG','NIFTY METAL':'^CNXMETAL','NIFTY MEDIA':'^CNXMEDIA','NIFTY REALTY':'^CNXREALTY','NIFTY PRIVATE BANK':'^CNXPVTBANK','NIFTY PSU BANK':'^CNXPSUBANK','NIFTY OIL & GAS':'^CNXENERGY','NIFTY CAPITAL GOODS':'^CNXCG','NIFTY CEMENT':'^CNXCEMENT','NIFTY INFRASTRUCTURE':'^CNXINFRA','NIFTY ENERGY':'^CNXENERGY','NIFTY COMMODITIES':'^CNXCOMMODITIES'
};
const QUALITY:Record<string,number>={Diversified:78,'Large & Mid Cap':82,'Multi Asset':84,'Multi Cap':80,Infrastructure:78,'Banking & Financial':77,'Nifty 50':92,Healthcare:84,'Focused Equity':80,Children:80,'Small Cap':74,'Mid Cap':80,'Nifty Next 50':90,Gold:82,Value:85,ELSS:82,Services:78,Technology:86};
const H={'User-Agent':'Mozilla/5.0 (compatible; SmartMFDecisionTerminal/1.0)','Accept':'application/json,text/plain,*/*'};

async function getJson(url:string,extra:Record<string,string>={}){const c=new AbortController();const t=setTimeout(()=>c.abort(),6500);try{const r=await fetch(url,{cache:'no-store',signal:c.signal,headers:{...H,...extra}});if(!r.ok)throw Error(`HTTP ${r.status}`);return await r.json();}finally{clearTimeout(t);}}
async function getText(url:string){const c=new AbortController();const t=setTimeout(()=>c.abort(),6500);try{const r=await fetch(url,{cache:'no-store',signal:c.signal,headers:H});if(!r.ok)throw Error(`HTTP ${r.status}`);return await r.text();}finally{clearTimeout(t);}}
function dateKey(v:string){const p=v.split('-');if(p.length!==3)return v;const mm:Record<string,string>={Jan:'01',Feb:'02',Mar:'03',Apr:'04',May:'05',Jun:'06',Jul:'07',Aug:'08',Sep:'09',Oct:'10',Nov:'11',Dec:'12'};return `${p[2]}-${mm[p[1]]??p[1]}-${p[0].padStart(2,'0')}`;}
function norm(v:string){return v.toUpperCase().replace(/&/g,' AND ').replace(/[^A-Z0-9]+/g,' ').replace(/\s+/g,' ').trim();}
function ret(p:Point[]){const r=new Map<string,number>();for(let i=1;i<p.length;i++){const a=p[i-1].value,b=p[i].value;if(a>0&&b>0)r.set(p[i].date,(b/a-1)*100);}return r;}
function sma(p:Point[],n:number){return p.length>=n?p.slice(-n).reduce((a,x)=>a+x.value,0)/n:null;}
function pr(p:Point[],days:number){if(p.length<2)return null;const e=p.at(-1)!.value,s=p[Math.max(0,p.length-1-days)]!.value;return s>0?(e/s-1)*100:null;}
function beta(xs:number[],ys:number[]){if(xs.length<30)return null;const mx=xs.reduce((a,b)=>a+b,0)/xs.length,my=ys.reduce((a,b)=>a+b,0)/ys.length;let c=0,v=0;for(let i=0;i<xs.length;i++){c+=(xs[i]-mx)*(ys[i]-my);v+=(ys[i]-my)**2;}return v?Math.max(.1,Math.min(3,c/v)):null;}
function match(n:string,row:Record<string,unknown>){const a=norm(n),b=norm(String(row.index??row.indexName??row.name??''));return a===b||a===`${b} INDEX`||b===`${a} INDEX`||(a.length>8&&(a.includes(b)||b.includes(a)));}
async function amfi(){const m=new Map<string,{nav:number;date:string}>();try{const raw=await getText('https://www.amfiindia.com/spages/NAVAll.txt');for(const line of raw.split(/\r?\n/)){const p=line.split(';');if(p.length<6)continue;const code=p[0]?.trim()??'',nav=Number(p[4]?.trim()),date=p[5]?.trim()??'';if(/^\d+$/.test(code)&&Number.isFinite(nav)&&date)m.set(code,{nav,date});}}catch{}return m;}
async function fundHistory(code:string){try{const d=await getJson(`https://api.mfapi.in/mf/${code}`) as {data?:Array<{nav?:string;date?:string}>};return(Array.isArray(d.data)?d.data:[]).map(x=>({date:dateKey(String(x.date??'')),value:Number(x.nav)})).filter(x=>x.date&&Number.isFinite(x.value)).sort((a,b)=>a.date.localeCompare(b.date));}catch{return[];}}
async function idxHistory(symbol:string){try{const d=await getJson(`https://query1.finance.yahoo.com/v8/finance/chart/${encodeURIComponent(symbol)}?range=2y&interval=1d`) as {chart?:{result?:Array<{timestamp?:number[];indicators?:{quote?:Array<{close?:Array<number|null>}>}}>} };const r=d.chart?.result?.[0],ts=r?.timestamp??[],cl=r?.indicators?.quote?.[0]?.close??[];return ts.map((t,i)=>({date:new Date(t*1000).toISOString().slice(0,10),value:Number(cl[i])})).filter(x=>Number.isFinite(x.value));}catch{return[];}}
async function nseRows(){try{const home=await fetch('https://www.nseindia.com/',{cache:'no-store',headers:H});const cookie=home.headers.get('set-cookie')?.split(';')[0]??'';const d=await getJson('https://www.nseindia.com/api/allIndices',{'accept-language':'en-US,en;q=0.9','referer':'https://www.nseindia.com/market-data/live-market-indices',...(cookie?{cookie}:{})}) as {data?:unknown};return Array.isArray(d.data)?d.data.filter((x):x is Record<string,unknown>=>typeof x==='object'&&x!==null):[];}catch{return[];}}

function alignment(category:string,lead:string){const s=lead.toUpperCase();if(s.includes('IT')||s.includes('TELECOMMUNICATION'))return category==='Technology'?1:category==='Focused Equity'?.45:0.35;if(s.includes('HEALTH')||s.includes('PHARMA')||s.includes('HOSPITAL'))return category==='Healthcare'?1:category==='Focused Equity'?.45:0.35;if(s.includes('BANK')||s.includes('FINANCIAL')||s.includes('NBFC')||s.includes('INSURANCE'))return category==='Banking & Financial'?1:['Focused Equity','Value'].includes(category)?.5:0.35;if(s.includes('SMALLCAP'))return category==='Small Cap'?1:['Multi Cap','Diversified'].includes(category)?.4:0.25;if(s.includes('MIDCAP'))return category==='Mid Cap'?1:category==='Large & Mid Cap'?.7:['Multi Cap','Diversified'].includes(category)?.4:0.25;if(s.includes('INFRA')||s.includes('CAPITAL GOODS')||s.includes('POWER')||s.includes('CEMENT')||s.includes('CONSTRUCTION'))return category==='Infrastructure'?1:['Diversified','Multi Cap','Large & Mid Cap'].includes(category)?.4:0.25;if(s.includes('SERVICES'))return category==='Services'?1:['Diversified','Multi Cap','Large & Mid Cap'].includes(category)?.4:0.25;if(s.includes('COMMODITIES'))return category==='Gold'?1:0.15;if(s==='NIFTY 50')return category==='Nifty 50'?1:['Focused Equity','Diversified','Multi Cap','Large & Mid Cap','ELSS','Children','Value'].includes(category)?.45:0.25;return .3;}

function strategic(h:Point[],quality:number){const nav=h.at(-1)?.value??0,s20=sma(h,20),s50=sma(h,50),s100=sma(h,100),s200=sma(h,200),r1=pr(h,21)??0,r3=pr(h,63)??0,r6=pr(h,126)??0;const peak=Math.max(...h.map(x=>x.value),nav),dd=peak>0?(nav/peak-1)*100:0;const trend=((nav>=(s50??nav)?1:0)+(nav>=(s200??nav)?1:0)+((s50??nav)>=(s200??nav)?1:0))/3*100;const consistency=[r1,r3,r6].filter(x=>x>=0).length/3*100;const returnScore=Math.max(0,Math.min(100,50+r1*1.5+r3*.6+r6*.3));const momentum=Math.max(0,Math.min(100,50+r1*3+r3*1.2));const drawdownQuality=Math.max(0,Math.min(100,50+Math.abs(Math.min(0,dd))*1.2));const score=quality*.25+trend*.30+returnScore*.20+drawdownQuality*.10+consistency*.10+momentum*.05;return{score:Math.max(0,Math.min(100,score)),nav,s20,s50,s100,s200,r1,r3,r6,dd,trend,momentum};}

export async function buildDashboard(swRaw:number,owRaw:number){
 let sw=Number.isFinite(swRaw)?swRaw:60,ow=Number.isFinite(owRaw)?owRaw:40;const total=sw+ow||100;sw=sw/total*100;ow=ow/total*100;
 const [amfiMap,nse]=await Promise.all([amfi(),nseRows()]);
 const indices:IndexRow[]=await Promise.all(INDEXES.map(async([name,group])=>{const row=nse.find(x=>match(name,x));if(row){const value=Number(row.last??row.indexValue??row.value),change=Number(row.percentChange??row.percChange??row.changePercent??row.change);if(Number.isFinite(value)&&Number.isFinite(change))return{name,value,change,group,status:'LIVE',source:'NSE'};}if(Y[name]){const h=await idxHistory(Y[name]),a=h.at(-1),b=h.at(-2);if(a)return{name,value:a.value,change:b?(a.value/b.value-1)*100:0,group,status:'LIVE',source:'YAHOO'};}return{name,value:null,change:null,group,status:'UNAVAILABLE',source:'UNAVAILABLE'};}));
 const idxMap=new Map(indices.map(x=>[x.name,x]));
 const idxHist=new Map((await Promise.all(INDEXES.filter(([n])=>Y[n]).map(async([n])=>[n,await idxHistory(Y[n])] as [string,Point[]]))));
 const fundMap=new Map(await Promise.all(FUNDS.map(async f=>[f.code,await fundHistory(f.code)] as [string,Point[]])));
 const funds=FUNDS.map(f=>{const h=fundMap.get(f.code)??[],latest=amfiMap.get(f.code),nav=latest?.nav??h.at(-1)?.value??null,prev=h.at(-2)?.value??null,chg=nav!=null&&prev!=null?(nav/prev-1)*100:null,st=strategic(h,QUALITY[f.category]??75);const live=f.proxies.map(p=>idxMap.get(p)).filter((x):x is IndexRow=>Boolean(x&&x.status==='LIVE'&&x.change!=null));const lead=live.length?live.reduce((a,b)=>(b.change??0)<(a.change??0)?b:a):null;const marketMove=live.length?live.reduce((a,b)=>a+(b.change??0),0)/live.length:null;const falling=live.filter(x=>(x.change??0)<0).length;const confirmation=live.length?falling/live.length:0;const align=alignment(f.category,lead?.name??'');const rawFall=marketMove!=null&&marketMove<0?Math.min(100,Math.abs(marketMove)*38):0;const leadFall=lead?.change!=null&&lead.change<0?Math.min(100,Math.abs(lead.change)*45):0;const drawdown=Math.max(0,Math.min(100,50+Math.abs(Math.min(0,st.dd))*1.5));const relative=Math.max(0,Math.min(100,50+st.r1*3+st.r3*1.2));const daily=Math.max(0,Math.min(100,rawFall*.25+leadFall*.30*align+drawdown*.15+st.trend*.20+relative*.10));const structural=st.nav>0&&st.s50!=null&&st.s200!=null&&st.nav<st.s200&&st.s50<st.s200&&st.r3<0;const healthy=!structural&&confirmation>=.33&&marketMove!=null&&marketMove<0&&st.trend>=50;const final=Math.max(0,Math.min(100,st.score*sw/100+daily*ow/100));let classification=structural?'STRUCTURAL BREAKDOWN':healthy?'HEALTHY CORRECTION':'NEUTRAL / WAIT';let signal=structural?'AVOID TODAY':final>=72?'BUY ON DIP':final>=55?'SIP / ACCUMULATE':final>=40?'WATCH':'WAIT';let b:null|number=null;const ih=f.proxies.map(p=>idxHist.get(p)).find(x=>x&&x.length>30);if(ih&&h.length>30){const fr=ret(h),ir=ret(ih),xs:number[]=[],ys:number[]=[];for(const [d,v] of fr){const q=ir.get(d);if(q!=null){xs.push(v);ys.push(q);}}b=beta(xs,ys);}const expected=marketMove!=null&&b!=null?marketMove*b:null,base=prev??(nav!=null&&chg!=null?nav/(1+chg/100):null),estimate=base!=null&&expected!=null?base*(1+expected/100):null;const risk=Math.max(0,Math.min(100,(Math.abs(Math.min(0,st.dd))*1.8)+(100-st.trend)*.5));const confidence=Math.min(100,55+confirmation*25+(live.length/Math.max(1,f.proxies.length))*15);return{code:f.code,name:f.name,category:f.category,nav,previousNav:prev,change:chg,date:latest?.date??h.at(-1)?.date??null,status:nav!=null?'LIVE':'UNAVAILABLE',sectorMove:marketMove,leadSector:lead?.name??'NO LIVE MAPPED INDEX',leadMove:lead?.change??null,relativeCorrection:expected!=null&&marketMove!=null?expected-marketMove:null,confirmation,mappedLive:live.length,mappedTotal:f.proxies.length,sectorScore:Number((Math.max(rawFall,leadFall)*align).toFixed(1)),relativeScore:Number(relative.toFixed(1)),confirmationScore:Number((confirmation*100).toFixed(1)),qualityScore:QUALITY[f.category]??75,score:Number(final.toFixed(1)),opportunityScore:Number(final.toFixed(1)),riskScore:Number(risk.toFixed(1)),confidenceScore:Number(confidence.toFixed(1)),signal,reason:structural?'Structural breakdown filter triggered: NAV below 200DMA, 50DMA below 200DMA and 3M return negative.':healthy?`${lead?.name??'Mapped proxy'} is weak while fund trend remains intact: Healthy Correction.`:`Final Daily Score = ${sw.toFixed(0)}% Strategic + ${ow.toFixed(0)}% Daily NAV Opportunity.`,historicalCheapness:Number(drawdown.toFixed(1)),volatility:null,drawdown:Number(st.dd.toFixed(1)),beta:b==null?null:Number(b.toFixed(2)),dataConfidence:Math.round(live.length/Math.max(1,f.proxies.length)*100),strategicScore:Number(st.score.toFixed(1)),dailyNavOpportunityScore:Number(daily.toFixed(1)),finalDailyScore:Number(final.toFixed(1)),classification,trend:{navVs50:st.s50!=null?st.nav>=st.s50:null,navVs200:st.s200!=null?st.nav>=st.s200:null,sma50Vs200:st.s50!=null&&st.s200!=null?st.s50>=st.s200:null,s20:st.s20,s50:st.s50,s100:st.s100,s200:st.s200},momentum:{r1:Number(st.r1.toFixed(2)),r3:Number(st.r3.toFixed(2)),r6:Number(st.r6.toFixed(2)),relativeStrength:Number(relative.toFixed(1))},estimatedNav:estimate,estimatedChange:expected,proxyBasket:true};}).sort((a,b)=>b.finalDailyScore-a.finalDailyScore);
 const live=indices.filter(x=>x.status==='LIVE'),sector=indices.filter(x=>x.group==='SECTORAL'),falling=live.filter(x=>(x.change??0)<0),greenSector=sector.filter(x=>(x.change??0)>0).length,breadth=sector.length?greenSector/sector.length*100:0,up=live.length?live.filter(x=>(x.change??0)>0).length/live.length*100:0,regime=breadth>=55&&up>=50?'RISK ON':'RISK OFF';
 const sectors=indices.filter(x=>x.group!=='BROAD').map(x=>({name:x.name,value:x.value,change:x.change,status:x.status,severity:x.change!=null&&x.change<0?'WEAK':'STRONG'}));
 return{updatedAt:new Date().toISOString(),weights:{strategic:sw,opportunity:ow},funds,indices,sectors,summary:{trackedFunds:19,liveFunds:funds.filter(x=>x.status==='LIVE').length,liveIndices:live.length,fallingIndices:falling.length,buySignals:funds.filter(x=>x.signal==='BUY ON DIP').length,accumulateSignals:funds.filter(x=>x.signal==='SIP / ACCUMULATE').length,breadth:Number(breadth.toFixed(1)),regime,regimeNote:regime==='RISK ON'?'Continue SIP + deploy corrections selectively.':'Continue core SIP, reduce tactical allocation, avoid chasing sector funds.',dataConfidence:Math.round(live.length/Math.max(1,indices.length)*100),coverage:`${live.length}/${indices.length}`},disclaimer:'This dashboard provides probability-based decision signals, not investment advice or a guaranteed NAV prediction. Today’s market weakness is likely to be reflected in the fund’s closing NAV; never interpret this as a guaranteed NAV move. Applicable NAV depends on the applicable cut-off and fund-realisation conditions. Sector/proxy mapping is an approximation, not the fund’s actual portfolio holdings. Free/unofficial data may be delayed, incomplete, or temporarily unavailable. The 2:30 PM window is this terminal’s tactical snapshot time, not a claim about the applicable AMFI cut-off for every scheme.'};
}
